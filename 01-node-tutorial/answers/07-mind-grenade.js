const num1 = 5;
const num2 = 10;

// Define a function that multiplies the values and logs the result
function multiplyValues() {
  console.log(`The product is: ${num1 * num2}`);
}

// Call the function to demonstrate execution
multiplyValues();